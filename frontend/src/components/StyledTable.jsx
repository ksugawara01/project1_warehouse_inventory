import styled from "styled-components"

const FlexDiv = styled.div`
    display: flex;
    align-items: center;
`

const StyledTable = styled.table`
    border-bottom: 1px solid black;
    border-collapse: collapse;
    margin-top: 30px;
    text-align: center;
`

export { StyledTable, FlexDiv}